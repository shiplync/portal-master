"use strict";

angular.module('shippersPortalApp.team.divisions')
.controller('divisionsDetailController', function($scope, $stateParams, $state, $q, divisionsService){
  $scope.init = function() {
    
  }

  // Currently all functionality resides in divisions.settings

})